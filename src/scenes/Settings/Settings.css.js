// @flow
import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  input: {
    borderColor: '#7a42f4',
    borderWidth: 1,
    height: 50,
    width: '100%'
  }
});

export { styles };
